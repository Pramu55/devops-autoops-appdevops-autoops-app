apiVersion: v1
kind: Service
metadata:
  name: devops-app
  namespace: devops-autoops
  labels:
    app: devops-app
spec:
  type: NodePort
  selector:
    app: devops-app
  ports:
    - protocol: TCP
      port: 3000
      targetPort: http
