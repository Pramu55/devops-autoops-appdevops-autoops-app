apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: devops-app-ingress
  namespace: devops-autoops
spec:
  podSelector:
    matchLabels:
      app: devops-app
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: 3000
